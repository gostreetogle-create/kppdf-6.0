-- ============================================================
-- POSTGRESQL INIT MIGRATION (PLACEHOLDER)
-- ============================================================
--
-- Cycle v3.4 — FINAL migration to PostgreSQL.
-- Project was on SQLite during early development cycles,
-- now permanently migrated to PostgreSQL.
--
-- How to generate the real migration:
--   1. Ensure DATABASE_URL points to a PostgreSQL instance
--   2. Run: npx prisma migrate dev --name init_postgresql
--   3. This will:
--      - Drop the old 20260620185844_init_sqlite_clean migration
--      - Generate full PostgreSQL schema from schema.prisma
--      - Apply it to the database
--      - Update this migration.sql with the actual SQL
--
-- All 47 models from schema.prisma will be created.
-- All IDs use cuid() — compatible with PostgreSQL TEXT/CHAR(25).
--
-- ⚠️ DO NOT hand-write this file. Run `prisma migrate dev`.
-- ============================================================
--
-- This is a placeholder. The real SQL will be generated by:
--   npx prisma migrate dev --name init_postgresql
--
-- Until then, for dev/testing without a PostgreSQL instance,
-- use: npx prisma db push
--
