# Модуль «Финансы / Закрытие заказа» (OrderClosing)

> Этот документ — каркас описания модуля «Финансы / Закрытие заказа». Описывает финальный этап цепочки: после производства и отгрузки — фиксация оплаты, сверка счётов и закрытие сделки. Документ намеренно короткий, чтобы задать контуры — детализация будет позже, по приоритету, как и для всего проекта.
>
> Согласовано с `МОДУЛЬ-КОММЕРЧЕСКОЕ-ПРЕДЛОЖЕНИЕ.md`: статус КП «оплачено» → переход в финансовый учёт. Согласовано с `МОДУЛЬ-ДОГОВОР.md`: после подписания договора контроль оплаты переходит в финансы. Согласовано с `МОДУЛЬ-ПРОИЗВОДСТВО.md`: факт отгрузки запускает сверку. Согласовано с `МОДУЛЬ-СКЛАД.md`: списание со склада = себестоимость для финансов.
>
> **Что НЕ делаем в первой версии (границы):** см. раздел 8 в конце. Топ-3 ограничения: нет интеграции с банком/1С, нет частичных оплат по этапам, нет авто-перевыставления счетов.

---

## 0. Глоссарий

| Термин | Что значит |
|---|---|
| **Заказ (Order)** | Сквозная сущность «от КП до оплаты». Один Order = один КП (или одно КП → один Договор → один/несколько ЗП → одна Отгрузка → один/несколько Платёж). Создаётся автоматически при конвертации КП в договор. |
| **Счёт (Invoice)** | Документ выставления оплаты клиенту. Содержит позиции, сумму к оплате, срок. Может быть авансовым или финальным. |
| **Платёж (Payment)** | Факт поступления денег от клиента. Привязан к Счёту (один Счёт → один или несколько Платежей — частичная оплата). |
| **Закрытие заказа (OrderClosing)** | Процедура финальной сверки: все позиции отгружены, все счета оплачены, остаток = 0. После закрытия заказ уходит в архив для аналитики. |
| **Сальдо (Balance)** | Сумма разницы между выставленным (по счетам) и полученным (по платежам). Отрицательное = клиент должен. Положительное = переплата. |
| **Себестоимость заказа** | Суммарная себестоимость всех позиций, рассчитанная модулем Склад при отгрузке. Используется для маржинальности. |

---

## 1. Для чего нужен этот модуль

**Финансы / Закрытие заказа** — это финальный этап жизненного цикла сделки. Модуль решает четыре задачи:

1. **Фиксация оплаты** — при поступлении денег от клиента менеджер (или бухгалтер, в зависимости от роли) регистрирует платёж.
2. **Контроль дебиторки** — на любой момент времени видно, сколько каждый клиент должен и по каким счетам.
3. **Расчёт маржинальности** — сравнение цены продажи (из КП/Договора) и себестоимости (из Склада). Без этого непонятно, сколько мы реально заработали.
4. **Закрытие заказа** — формальная процедура, которая гарантирует: всё отгружено, всё оплачено, заказ можно передать в архив.

Без этого модуля мы получим ситуацию: КП сделано, договор подписан, производство выполнено, товар отгружен — а бухгалтерия не знает, пришли ли деньги, и сколько мы заработали.

---

## 2. Для кого он работает

| Роль | Что делает в модуле |
|---|---|
| **Менеджер по продажам** | Видит свой пайплайн: какие счета ждут оплаты, какие КП в архиве. Не имеет права редактировать платежи. |
| **Бухгалтер** | Регистрирует входящие платежи, сверяет с банковской выпиской (вручную, в первой версии), формирует итоговые отчёты. |
| **Руководитель / директор** | Видит сводную аналитику: маржинальность по сделкам, дебиторка по клиентам, динамика выручки. |
| **Администратор** | Настраивает справочники (ставки НДС, валюты, типы счетов), если такие сущности понадобятся в первой версии. |

---

## 3. Жизненный цикл

### Статусы Заказа (Order)

```
        ┌─────────────────┐
        │ Черновик        │  ← авто-создаётся при подписании Договора (Триггер: Contract.status='SIGNED', СПОР-5)
        └────────┬────────┘
                 │ (договор подписан → Договор в статусе «Подписан»)
                 ▼
        ┌─────────────────┐
        │ В работе        │  ← идёт производство / ожидаем отгрузку
        └────────┬────────┘
                 │ (отгрузка выполнена → Склад: все позиции shipped)
                 ▼
        ┌─────────────────┐
        │ Ожидает оплаты  │  ← выставлен финальный счёт
        └────────┬────────┘
                 │ (сальдо = 0)
                 ▼
        ┌─────────────────┐
        │ Закрыт          │  ← всё оплачено, заказ уходит в архив
        └─────────────────┘

  В любом статусе (после DRAFT): можно перевести в «Отменён» (CANCELLED — добавлено 24.06.2026, правка #3 МАСТЕР-АУДИТ-V6.md)
  → [CANCELLED] ← ЗК отменён + Refund на всю или частичную сумму; необратимо (order уходит в архив для аудита)
```

### Таблица переходов

| Статус | Следующий | Триггер | Кто может перевести |
|---|---|---|---|
| Черновик | В работе | Договор подписан клиентом | Автоматически (при изменении статуса Договора) |
| В работе | Ожидает оплаты | Склад пометил все позиции как «отгружено» | Автоматически |
| Ожидает оплаты | Закрыт | Сальдо = 0 | Бухгалтер (или автоматически при заведении платежа) |
| Закрыт | — | (архив) | — |
| (Любой после DRAFT) | **CANCELLED** | ЗК отменён + Refund на всю/часть суммы (добавлено 24.06.2026 — правка #3 из МАСТЕР-АУДИТ-V6.md) | Бухгалтер (через `Refund`) |

### Статусы Счёта (Invoice)

| Статус | Описание | Когда |
|---|---|---|
| **Черновик** | Счёт создан, но не выставлен | Менеджер/бухгалтер готовит счёт |
| **Выставлен** | Счёт отправлен клиенту | После нажатия «Отправить счёт» |
| **Частично оплачен** | Поступила часть суммы | Один или несколько платежей < суммы счёта |
| **Полностью оплачен** | Сальдо = 0 | Все платежи поступили |
| **Просрочен** | Срок оплаты прошёл, сальдо > 0 | Платёж не поступил до dueDate |

---

## 4. Ключевой сценарий: «От подписания договора до закрытия заказа»

> **Триггер Order:** `Contract.status='SIGNED'` (СПОР-5). При подписании Договора автоматически создаётся Order в статусе `DRAFT` → авто-переход `IN_PROGRESS` через 1 секунду. Если Договор расторгнут (`Contract.status='TERMINATED'`) — Order переходит в `CANCELLED`.

**Явная цепочка связей между модулями** (для разрешения противоречия #6 из `МАСТЕР-АУДИТ-V6.md`):

```
[КП]                            [Договор]                        [Финансы]
number=КП-XXXX               number=Д-XXXX                    number=Order-XXXX
parentProposalId ───────────► parentProposalId  ─────────────► proposalId
                              contractId  ────────────────────► contractId
parentProposalId (тот же) ───► parentContractId (опц.) ───────► (нету)
customerId ──────────────────► customerId ────────────────────► customerId
contractorId ─────────────────► contractorId ──────────────────► contractorId
```

1. **Договор подписан** (Договор переходит в статус «Подписан», см. `МОДУЛЬ-ДОГОВОР.md` §3).
   - **Триггер СПОР-5**: автоматически создаётся **Order** в статусе `draft` → авто-переход в `in_progress` через 1 секунду.
   - **Order связан ТОЛЬКО с одним Contract** (`contractId`, NOT NULL, ON DELETE RESTRICT) и через `Contract.parentProposalId` опосредованно с одним Proposal.
   - Order НЕ создаётся, если Конвертация КП из «Оплачено» была запрещена (правка B): Договор подписывается раньше «Оплачено», поэтому Order создаётся нормально.
2. **Бухгалтер выставляет счёт (Invoice).**
   - Первичный авансовый счёт (например, 50% от суммы договора, тип `ADVANCE`) или один счёт на всю сумму (тип `MAIN`).
   - `Invoice.orderId → Order.id` (FK NOT NULL, ON DELETE CASCADE).
   - `Invoice.status` = 'ISSUED' после нажатия «Отправить счёт».
3. **Бухгалтер регистрирует входящий платёж (Payment).**
   - Вручную выбирает `Payment.invoiceId` через dropdown «Привязать к счёту» (СПОР-7).
   - `Payment.type = 'INCOMING'`, `Payment.amount > 0`.
   - Сальдо пересчитывается: `Invoice.paidAmount += amount`, `Invoice.balance -= amount`. Если `balance = 0` → `Invoice.status = 'FULLY_PAID'`.
   - Если все Invoice в Order полностью оплачены → `Order.status` авто-`AWAITING_PAYMENT` → `CLOSED` (через 1 день задержки для возможных корректировок).
4. **Руководитель смотрит аналитику.**
   - **Маржа** = `Σ (цена продажи позиции × кол-во)` − `Σ costOfGoodsSold` (из `Shipment` через Склад). Услуги/работы — отдельная строка в марже без себестоимости.
   - **Дебиторка** = сумма отрицательных `Order.balance` по всем активным (`IN_PROGRESS` + `AWAITING_PAYMENT`) Order.
5. **Если ЗК отменён (крайний случай):** `ProductionOrder.status = 'CANCELLED'` → бухгалтер оформляет `Refund` (см. §6 правила Refund + §10 поля Refund). `Order` переходит в `CANCELLED` или остаётся `AWAITING_PAYMENT` с частичным `paidAmount`.

### Связь с предыдущими модулями

```
[МОДУЛЬ-КОММЕРЧЕСКОЕ-ПРЕДЛОЖЕНИЕ]   → статус «оплачено» → триггер для финансов
[МОДУЛЬ-ДОГОВОР]                     → подписан → авто-создание Order
[МОДУЛЬ-ПРОИЗВОДСТВО]                → все позиции выполнены → триггер для финансов
[МОДУЛЬ-СКЛАД]                       → все позиции shipped → передача себестоимости в финансы
[МОДУЛЬ-ФИНАНСЫ (этот)]              → фиксация оплаты → закрытие Order → маржа → архив
```

**⚠️ Уточнение (АНАЛИЗ-П1):** Триггер создания Order — **подписание Договора** (когда Contract.status = 'signed'). Order создаётся в статусе 'draft' и автоматически переходит в 'in_progress'. Счёт (Invoice) создаётся отдельно бухгалтером, уже привязанный к Order.

---

## 5. Экраны (UI)

### Экран 1: Список заказов `/finance/orders`

Трёхзонный макет Каркас-Kit (та же парадигма, что в КП):

| Зона | Что показывает |
|---|---|
| **Левая панель** | Фильтры (по статусу, по клиенту, по менеджеру, «просроченные счета»), быстрый поиск по номеру Order или Договора |
| **Центр** | Табличный список Order с колонками: № Order, Клиент, Менеджер, Сумма, Оплачено, Сальдо, Статус, Дата закрытия |
| **Правая панель** | Сводка: итого выставлено, итого получено, итого в дебиторке, график поступлений по месяцам |

### Экран 2: Карточка Order `/finance/orders/[id]`

| Зона | Что показывает |
|---|---|
| **Левая панель** | Хронология событий Order (подписание договора, выставление счетов, поступление платежей), ссылки на связанные документы (КП, Договор, ЗП) |
| **Центр** | Шапка Order (реквизиты клиента, наша организация, дата, сумма), **таблица счетов** (№, дата, сумма, статус, остаток), **таблица платежей** (дата, сумма, способ — ручной ввод в первой версии) |
| **Правая панель** | Действия (создать счёт, зарегистрировать платёж, закрыть заказ), сводка (сумма, оплачено, сальдо, маржа) |

---

## 6. Бизнес-правила

### RBAC (роли и права)

| Действие | Менеджер | Бухгалтер | Директор | Админ |
|---|---|---|---|---|
| Видеть свой список Order | ✅ | ✅ (всех) | ✅ (всех) | ✅ (всех) |
| Создавать/редактировать счёт | ❌ | ✅ | ✅ | ✅ |
| Регистрировать платёж | ❌ | ✅ | ✅ | ✅ |
| Закрывать заказ | ❌ | ✅ | ✅ | ✅ |
| Видеть маржинальность | ✅ (свою) | ✅ | ✅ (всех) | ✅ |
| Видеть дебиторку по клиенту | ❌ | ✅ | ✅ | ✅ |

### Правила расчёта

- **Сальдо = Сумма счёта − Σ Платежей по этому счёту.** Пересчитывается при каждом новом платеже.
- **Маржа заказа = Σ (цена продажи позиции × кол-во) − Σ себестоимости позиций из Склада.** Себестоимость доступна только когда позиция прошла через Склад (`shipped`).
- **Просроченный счёт** считается просроченным на следующий день после `dueDate` при сальдо > 0. В списке заказов выделяется красноватым.

### Правила целостности

- **Заказ нельзя закрыть, если хотя бы один счёт имеет сальдо > 0.** Кнопка «Закрыть заказ» заблокирована, рядом — список незакрытых счетов.
- **Платёж нельзя удалить**, только **сторнировать** (создать компенсирующий платёж `Payment.type='STORNO'`, `Payment.amount < 0`, `Payment.correctsPaymentId` FK на ошибочный платёж). Это сохраняет аудит-трейл. Сторно — **техническая корректировка опечатки бухгалтера**; деньги физически не двигались. Связка `original + STORNO` в сумме = 0.
- **Возврат денег клиенту (Refund) — отдельная сущность**, НЕ отрицательный `Payment`. Refund фиксирует **реальный возврат средств** при отменённом ЗК / возврате товара / браке. Связан с отменённым `ProductionOrder` или `Shipment`. `Order.paidAmount` математически = `Σ Payment.amount(type=INCOMING) − Σ Refund.amount`.

**Правила Сторно (Payment.type='STORNO'):**
1. **Нулевое влияние на бизнес-статусы**: ЗК/Договор/Order НЕ меняются. Это сугубо бухгалтерская операция исправления опечатки.
2. **Сальдо возвращается в исходное**: `Invoice.paidAmount` и `Order.balance` восстанавливаются (Σ INCOMING − Σ STORNO = Σ net payment).
3. **Запрет на цепочки**: Нельзя сделать STORNO на STORNO.
4. **Обязателен комментарий**: `Payment.notes NOT NULL` при type='STORNO' (почему был ошибочен).
5. **В UI** отображается рядом с ошибочным платежом серой/перечёркнутой строкой.

**Правила Refund (возврат клиенту):**
1. **Бизнес-триггер**: ЗК отменён (СПОР-12), клиент передумал, пришёл брак, возврат товара через `WriteOffAct.reason='DEFECT'`.
2. **Лимит суммы**: `Σ Refund.amount` по конкретному `originalPaymentId` ≤ суммы самого платежа (полный или частичный возврат).
3. **Изменение статуса Order**: Если проведён Refund на всю оставшуюся сумму при отменённом ЗК → `Order.status = 'CANCELLED'` (спецстатус, расширение enum).
4. **Связь с источником**: `Refund.linkedProductionOrderId?` (отмена ЗК), `Refund.linkedShipmentId?` / `Refund.linkedWriteOffId?` (возврат товара на склад).
5. **Обязательна причина**: `Refund.reason NOT NULL` (свободный текст, минимум 3 символа).

> **✅ СПОР-7 (24.06.2026, фиксация):** При регистрации платежа бухгалтер **вручную выбирает, к какому счёту (Invoice) привязан платёж** через dropdown «Привязать к счёту». Если клиент прислал платёж на несколько счетов — бухгалтер вводит несколько Payment, по одному на счёт, или один Payment без указания invoiceId (привязка к Order в целом). Авто-FIFO (аванс → финальный) отложено в v2. Связь 1 Payment ↔ 1 Invoice базовая; 1 Payment ↔ N Invoice — для частичных оплат.

### Связь с предыдущими модулями

- **Order ↔ КП:** при закрытии Order — КП остаётся в статусе «оплачено». КП **не возвращается в «принято»**.
- **Order ↔ Договор:** если Договор расторгнут, Order переходит в **спецстатус «Расторгнут»** (не «Закрыт»). Частично оплаченные счета отмечаются отдельно для разбирательства.
- **Order ↔ Склад:** себестоимость берётся только для **фактически отгруженных** позиций. Если в КП была услуга (монтаж) — отдельная строка в марже, но без себестоимости.
- **Order ↔ Финансы:** если в Order попала позиция со **сложной составной себестоимостью** (сборный товар с модулями) — берётся **итоговая себестоимость** из модуля Склад (уже агрегированная после производства).

---

## 7. Что в первой версии, а что нет

### ✅ Делаем в v1

- Список заказов с фильтрами и поиском.
- Карточка Order с таблицами счетов и платежей.
- Создание счёта вручную (бухгалтер/директор).
- Регистрация платежа вручную (без банковской интеграции).
- Автоматический пересчёт сальдо.
- Расчёт маржинальности по отгруженным позициям.
- Закрытие заказа с проверкой сальдо.
- Базовый отчёт: дебиторка по клиентам, маржа по менеджерам.

### ❌ Не делаем в v1

- **Интеграция с банком / 1С / банк-клиентом.** Все платежи — ручной ввод по выписке.
- **Частичные оплаты по этапам** (предоплата 30% → производство → доплата 70% → отгрузка). В v1 только один финальный счёт или простой аванс.
- **Авто-перевыставление счетов** при просрочке. Только красная подсветка, менеджер сам решает.
- **Финансовая аналитика** (NPV, графики помесячно, прогноз поступлений). Только списки и простые сводки.
- **Курсы валют.** В v1 работаем только в одной валюте (₽). Мультивалюта — отдельная задача.

> **✅ СПОР-14 (24.06.2026, фиксация):** Поле `Order.currency` и `Invoice.currency` жёстко `'RUB'` в v1 (default в Prisma-схеме). UI не показывает dropdown выбора валюты. Мультивалютность (USD/EUR/KZT) отложена в v2 — потребует доработку Payment.amount (валюта) и финансовых отчётов (конвертация по курсу).
- **Возврат денег (refund).** Реализуется через отдельную сущность `Refund` с положительной суммой (вычитается математически), см. §6 и §10 «Поля Refund». Сторно (через отрицательный `Payment.type='STORNO'`) — отдельный сценарий для ошибочных обычных платежей. Два механизма разделены явно.

---

## 8. Открытые вопросы — ✅ ВСЕ 8 ЗАКРЫТЫ (24.06.2026)

> Все 8 финансовых Q-вопросов получили ✅ РЕШЕНО. Источник — `OPEN-QUESTIONS-MASTER.md` раздел 1 (🔴 P0 по финансам) + раздел 2 (🟡 P1 дополнительно). Каждое решение применено в §3, §6, §10 выше.

| # | Вопрос | ✅ Решение | Где зафиксировано в этом модуле |
|---|---|---|---|
| **Q1** | Типы счетов | **A: Аванс + Основной + Корректировочный** | §10 «Поля Invoice» + `SCHEMA-CONSOLIDATED.md` §3.12 enum InvoiceType |
| **Q2** | Учёт НДС | **A: авто разделение по `vatRate` Договора** | §10 «Поля Invoice» + §6 правила расчёта |
| **Q3** | Принудительное закрытие заказа | **A: director + accountant двухступенчато** | §6 RBAC-таблица + §3 «Кто может перевести» |
| **Q4** | Order ↔ банковская выписка | **A: только ручной ввод в v1, загрузка PDF — v2** | §7 ❌ Не делаем в v1 |
| **Q5** | Отрицательные позиции в счёте | **A: только через Refund (отдельная сущность)** | §6 правила Refund + §10 «Поля Refund» + GAP-023 ✅ РЕШЕНО |
| **Q6** | Видимость маржинальности менеджером | **A: НЕТ в v1 (только director/accountant/admin)** | §6 RBAC-таблица + GAP-014 ✅ РЕШЕНО |
| **Q7** | Дата оплаты для учёта | **A: `receivedAt` (дата поступления на р/с)** | §10 «Поля Payment» — два поля: `receivedAt` (fakt) + `registeredAt` (systems) |
| **Q8** | Refund в v1 | **A: через отдельную сущность Refund (НЕ Сторно)** | §6 правила Сторно + Refund + §10 «Поля Refund» + GAP-023 ✅ РЕШЕНО + правка #3 из МАСТЕР-АУДИТ-V6.md |

**Все 8 Q закрыты.** Phase 1 Bootstrap по Модулю Финансы разблокирован.

---

## 9. Связи с другими модулями (полная картина)

```
┌───────────────────┐
│ КП (статус        │
│ «оплачено»)       │──────┐
└───────────────────┘      │
                           ▼
┌───────────────────┐   ┌───────────────────┐
│ Договор           │──▶│ Order (Черновик)  │
│ «Подписан»        │   │ contractId        │
└───────────────────┘   │ proposalId        │
                        └─────────┬─────────┘
                                  │ (автоматически: В работе)
                                  ▼
                        ┌───────────────────┐
                        │ Производство      │
                        │ все ЗП «выполнено»│
                        └─────────┬─────────┘
                                  ▼
                        ┌───────────────────┐
                        │ Склад             │───── себестоимость ─────▶│
                        │ все позиции       │                          │
                        │ «shipped»         │                          ▼
                        └─────────┬─────────┘              ┌───────────────────┐
                                  │                        │ Финансы            │
                                  ▼                        │ • счёт Invoice      │
                        ┌───────────────────┐              │ • платёж Payment    │
                        │ Order             │────────────▶│ • сальдо            │
                        │ «В работе»        │              │ • маржа (cost из    │
                        │ → ожидает оплаты  │              │   Склад)            │
                        └───────────────────┘              │ → «Закрыт»          │
                                                           └───────────────────┘
```

---

## 10. Поля данных (контракт сущностей)

### Order

| Поле | Тип | Откуда приходит | Зачем |
|---|---|---|---|
| `orderId` | `Order-XXXX` | авто-генерация при создании | Уникальный номер |

> **✅ СПОР-13 (24.06.2026, фиксация):** У каждого финансового типа (Order, Invoice, Payment) — **отдельный независимый счётчик нумерации.** Counter-таблица в БД хранит пары `{type, value}` отдельно для каждого типа документа. Это означает, что `Order-0001`, `Inv-0001`, `Pay-0001` могут иметь разные значения `value` для одного клиента/Order — это нормально. Confirmed: `archive/MIGRATION-PLAN-Phase1.md` подтверждает что Counter уже в v5 baseline.
| `proposalId` | FK → KP | из Договора | Связь вверх по цепочке |
| `contractId` | FK → Договор | из Договора | Связь вверх по цепочке |
| `customerId` | FK → Клиент | из КП | Кто платит |
| `contractorId` | FK → Наша организация | из Договора | Кому платят |
| `status` | enum | вычисляется автоматически | Текущий статус |
| `totalAmount` | decimal | из КП (цена продажи) | Сумма заказа |
| `paidAmount` | decimal | Σ платежей | Сколько уже пришло |
| `balance` | decimal | `totalAmount - paidAmount` | Сколько ещё ждать |
| `margin` | decimal | `totalAmount - costOfGoodsSold` | Маржа (только для shipped) |
| `closedAt` | timestamp | авто | Когда закрыт |
| `createdAt` | timestamp | авто | Когда создан |

### Invoice

| Поле | Тип | Зачем |
|---|---|---|
| `invoiceId` | `Inv-XXXX` | Уникальный номер |
| `orderId` | FK → Order | К какому заказу |
| `issuedAt` | date | Когда выставлен |
| `dueDate` | date | Крайний срок оплаты |
| `amount` | decimal | Сумма счёта |
| `paidAmount` | decimal | Сколько поступило |
| `balance` | decimal | Сколько ещё ждать |
| `status` | enum | Черновик / Выставлен / Частично / Полностью / Просрочен |
| `invoiceType` | enum | Аванс / Основной / Корректировочный |

### Payment

| Поле | Тип | Зачем |
|---|---|---|
| `paymentId` | `Pay-XXXX` | Уникальный номер |
| `type` | enum (INCOMING \| STORNO) | Обязательно. Default = INCOMING |
| `orderId` | FK → Order | К какому заказу |
| `invoiceId` | FK → Invoice (опц.) | К какому счёту (если не связано — в кучу Order) |
| `correctsPaymentId` | FK → Payment (опц.) | Только для type=STORNO: ссылка на ошибочный платёж. ON DELETE RESTRICT. |
| `receivedAt` | date | Когда деньги фактически пришли |
| `registeredAt` | timestamp | Когда бухгалтер внёс в систему |
| `amount` | decimal | type=INCOMING ⇒ > 0. type=STORNO ⇒ строго < 0 |
| `method` | enum (ручной ввод v1) | Безнал / нал / карта |
| `notes` | text | NOT NULL при type=STORNO |
| `createdById` | FK → User | Кто внёс |

### Refund (возврат клиенту — отдельная сущность, GAP-023 ✅ РЕШЕНО)

| Поле | Тип | Зачем |
|---|---|---|
| `id` | `Ref-XXXX` | Уникальный номер (отдельный счётчик Counter.type='refund') |
| `orderId` | FK → Order | ON DELETE RESTRICT |
| `originalPaymentId` | FK → Payment | ON DELETE RESTRICT |
| `linkedProductionOrderId` | FK → ProductionOrder (опц.) | ON DELETE SET NULL |
| `linkedShipmentId` | FK → Shipment (опц.) | ON DELETE SET NULL |
| `linkedWriteOffId` | FK → WriteOffAct (опц.) | ON DELETE SET NULL |
| `amount` | decimal | Сумма возврата (строго > 0). Вычитается математически |
| `reason` | String NOT NULL | ≥ 3 символа |
| `processedAt` | DateTime | Когда фактически вернули деньги |
| `registeredAt` | DateTime | Когда бухгалтер внёс |
| `createdById` | FK → User | Кто оформил |

**Расширения enum:**
- `Order.status += 'CANCELLED'` (для «ЗК отменён + Refund на всю сумму»). Итоговый: `DRAFT \| IN_PROGRESS \| AWAITING_PAYMENT \| CLOSED \| CANCELLED`.
- `Contract.status += 'TERMINATED'` (для «Договор расторгнут до завершения обязательств»). Итоговый: `DRAFT \| SENT \| SIGNED \| IN_PROGRESS \| COMPLETED \| ARCHIVED \| TERMINATED`.

---

## 11. Сценарии, которые нужно проверить в ИИ-симуляции

> Этот раздел — подсказка для прогона по `ВЕРИФИКАЦИЯ-ЧЕКЛИСТ.md`. По этим сценариям симулятор должен найти конкретные пробелы.

- **SC-01:** Клиент оплатил 80% суммы, остаток обещал через неделю. Можно ли закрыть заказ? *(должно быть «нет»).*
- **SC-02 (v1 канон):** Бухгалтер ошибся и внёс платёж с неправильной суммой (забыл ноль). **Процедура сторнирования:** открыть исходный `Payment` → нажать «Сторнировать» → указать причину в `notes` → создаётся новый `Payment` с `type='STORNO'`, `amount < 0`, `correctsPaymentId` FK → Сальдо возвращается в исходное → бухгалтер вводит правильный платёж. UI-flow подробно в §6 правила Сторно.
- **SC-03 (v1 канон):** Договор расторгнут (не подписан или расторгнут после подписания), но клиент уже заплатил аванс. **Что с Order:** `Contract.status = 'TERMINATED'` (новый спецстатус в v1). Order автоматически переходит в `CANCELLED`. Бухгалтер оформляет `Refund` на всю полученную сумму (если деньги реально возвращены клиенту). Если деньги удержаны по условиям договора → Order закрывается через `CLOSED + notes='Договор расторгнут, удержание по п. X.Y'` (для аудита).
- **SC-04:** КП содержало составной товар (из 5 модулей), себестоимость которого собирается только после производства. Как до этого момента считается маржа? *(должна быть `null` или «не рассчитана» — не додумывать).*
- **SC-05:** В Order попала услуга «Монтаж» (нет себестоимости). Маржа считается только по товарам или по всему заказу? *(по товарам — услуги вычитать не из чего).*
- **SC-06:** Один Order, два счёта (аванс + финальный). Платёж пришёл один на всю сумму. Как система распределит — на первый счёт или второй? *(требует правила).*
- **SC-07:** Менеджер уволился. Что происходит с его активными Order? *(переназначение — ответственность директора).*

---

**Связанные документы:**

- `МОДУЛЬ-КОММЕРЧЕСКОЕ-ПРЕДЛОЖЕНИЕ.md` — источник `Proposal`, `proposalId` FK в Order, признак «оплачено» (триггер создания ЗК).
- `МОДУЛЬ-ДОГОВОР.md` — источник `Contract`, **`contractId` FK в Order (NOT NULL, RESTRICT)**, триггер создания Order при `Contract.status='SIGNED'` (СПОР-5).
- `МОДУЛЬ-ПРОИЗВОДСТВО.md` — триггер перехода Order «В работе» → готовность к отгрузке. При `ProductionOrder.status='CANCELLED'` — триггер для `Refund`.
- [`../04_Склад/МОДУЛЬ-СКЛАД.md`](../04_Склад/МОДУЛЬ-СКЛАД.md) (обзорный каркас) → [`МОДУЛЬ-СКЛАД-ПОДРОБНЫЙ.md`](../04_Склад/МОДУЛЬ-СКЛАД-ПОДРОБНЫЙ.md) — источник `Shipment.costOfGoodsSold` для маржи в OrderClosing.
- `SCHEMA-CONSOLIDATED.md` — единая схема БД всех 32 сущностей + ON DELETE + ENUM-ы (включая `Payment.type='INCOMING'|'STORNO'` + отдельную сущность `Refund`).

---

## ЗК «Отменён» → КП остаётся «Оплачено» + Refund

При отмене ЗК (`ProductionOrder.status='CANCELLED'`) выполняются два независимых действия:

1. **КП остаётся «Оплачено»** в статусе `paid`. КП/Договор/ЗК/Order живут независимо — минимум статусов между ними (нет транзитивных переходов «оплачено → архив»). Аналитика — через JOIN по FK в отчётах.
2. Если ЗК отменён ПОСЛЕ получения оплаты от клиента — бухгалтер создаёт `Refund` (отдельная сущность, `amount > 0`, явная причина `reason ≥ 3 симв`). Деньги клиента возвращаются через формулу `Order.paidAmount -= Refund.amount`. Если других активных ЗК по тому же Order нет — `Order.status` переводится в `CANCELLED`.

Согласовано PO 24.06.2026 (Production Q4 + Finance Q5/Q8). Подробнее — `МОДУЛЬ-ПРОИЗВОДСТВО.md` §10 Q4; `OPEN-QUESTIONS-MASTER.md` Q4 раздела «Производство» + Q5/Q8 раздела «Финансы».

