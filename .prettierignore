# Auto-generated by Prisma (recreated by `pnpm prisma generate`)
src/generated/

# Next.js build artifacts
.next/
out/
next-env.d.ts

# TypeScript build info (auto-regenerated by `pnpm tsc`)
*.tsbuildinfo

# Local env files (gitignored anyway)
.env
.env.*
!.env.example

# pnpm
.pnpm-store/

# Misc
coverage/
playwright-report/
test-results/

# Build outputs + IDE files
dist/
*.min.js
*.bundle.js
.idea/
*.swp

# TODO (next session): before first .ts commit, run
#   pnpm exec prettier --write . && commit as `chore(format): apply defaults`
# Reason: .prettierrc `printWidth:100` + `trailingComma:"all"` will silently
# reformat 30+ files on first code touch, polluting git history.
# See code-reviewer round 2 (commit 9af5648) for full context.
